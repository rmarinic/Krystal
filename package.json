{
  "name": "krystal-tauri",
  "version": "0.15.1",
  "private": true,
  "description": "Krystal — a local Claude Code chat app, as a native Windows (Tauri) desktop app.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "test": "node tests/agents-store.test.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
