{
  "name": "krystal-tauri",
  "version": "0.13.1",
  "private": true,
  "description": "Krystal — a local Claude Code chat app, as a native Windows (Tauri) desktop app.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
